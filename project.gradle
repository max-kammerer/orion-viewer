ext {
    thirparty_build_root = rootDir.canonicalPath + "/../thirdparty/"
    mupdf = thirparty_build_root + "/mupdf/"
    djvu = thirparty_build_root + "/djvu/"

    mupdf_libs = mupdf + '/platform/android/libs'
    djvu_libs = djvu + '/android/libs/'
}